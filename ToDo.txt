
NEXT: complete capturing the inputs, assertinitialized, .process. and issue outputs.
-- Look at how JK-HighSeq.cpp stores its captured inputs (it should be in the Sequencers)

        captureParameters();

		captureInputs();

		highSeqModule.assertInitialized();

//		DEBUG ("masterStep %d", module.getMasterStep());
		debug_s (highSeqModule, "after Capture Inputs and Parameters()", highSeqModule.getMasterStep(), true);

		highSeqModule.process();

		debug_s (highSeqModule, "after Process", highSeqModule.getMasterStep(), true);

		outputActive();
		outputReset();
		outputLights();
		outputMaster();
